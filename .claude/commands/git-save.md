幫我執行以下步驟：
1. git status ，然後檢查有沒有可能的敏感檔案被包含在 git 裡面，有的話更新 .gitignore
2. git add .
3. 根據這次的變更內容，自動產生一個有意義的 commit message（用 conventional commits 格式）
4. git commit
5. git push

不要問我 commit message，自己根據 diff 判斷。